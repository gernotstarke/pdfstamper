 // some actions
            aboutArc42 = sb.action(
                    name:        'About',
                    closure:     this.&showAbout,
                    mnemonic:    'A',
            )

            quitAction = sb.action(
                    name:       'Quit',
                    closure:    System.exit(0)
            )
            // our menu
            menuBar() {
                menu('PdfStamper'){
                    menuItem('About PdfStamper') {
                        // TODO add about-action
                    }
                    separator()
                    menuItem('Quit PdfStamper') {
                        action: quitAction
                    }

                }
                menu('File') {
                    menuItem('Select source directory') {
                        // TODO call open source directory
                    }
                    menuItem('Select target directory') {
                        //
                    }

                }
                glue()
                menu('Help') {
                    menuItem( 'PdfStamper Help') {

                    }
                    separator()
                    menuItem( action:aboutArc42)

                    menuItem( 'Licence')   {

                    }
                }

            }
